# Base URL where chat-widget.template.html is hosted (no trailing slash).
# Example: https://cdn.example.com/widgets
VITE_CHAT_WIDGET_TEMPLATE_HOST=

# API origin/path prefix for connector routes (same as web_dashboard).
VITE_API_URL=
